name: perfora_analyzer
description: Deterministic Dart evidence worker for Perfora.
version: 0.4.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  analyzer: ^7.4.5
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
